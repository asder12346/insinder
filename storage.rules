rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /blog_images/{imageId} {
      allow read: if true;
      allow write: if request.auth != null && (request.auth.token.email == 'samuelabedecornelius@gmail.com' || request.auth.uid != null);
    }
  }
}
